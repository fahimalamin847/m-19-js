let student = {
    name: 'Ariana Grande',
    age: 21,
    city: 'Gaibandha',
    isStudent: true
};

// Get an array of object keys and find its length
let numberOfProperties = Object.keys(student).length;

console.log("Number of Properties:", numberOfProperties);
